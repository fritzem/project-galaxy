export default class Viewport {
    constructor(originX, originY, width, height, zoom) {
        this.originX = originX;
        this.originY = originY;
        this.width = width;
        this.height = height;
        this.zoom = zoom;
    }
}
